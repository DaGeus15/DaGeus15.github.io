/**
 * Nivel de cada idioma, en porcentaje de la barra (0-100).
 * No se traduce; los nombres y las etiquetas están en `copy.js` y se cruzan
 * por la clave (`es` / `en` / `fr`), no por posición.
 */
export const languageLevels = {
  es: 100,
  en: 80,
  fr: 25,
};

export default languageLevels;
