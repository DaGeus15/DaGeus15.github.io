import { FiUser, FiBriefcase, FiCode, FiMail } from "react-icons/fi";

/**
 * Traduce las claves de `src/content/navigation.js` a componentes de icono.
 * Vive aquí para que los archivos de contenido sigan siendo datos puros.
 */
export const navIcons = {
  user: FiUser,
  briefcase: FiBriefcase,
  code: FiCode,
  mail: FiMail,
};

export default navIcons;
